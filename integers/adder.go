package integers

func Add(x, y int) int {
	return x + y
}

// when there are more than two args of the same type, they can be shortened as (arg1, arg2 type) - so (x, y int) rather than (x int, y int)
